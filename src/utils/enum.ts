export enum PAGE {
    CURRENT = 1,
    SIZE = 10
}

export enum AUTH {
    _7_DAYS = 7 * 24 * 60 * 60 * 1000
}

export enum STATUS {
    ACTIVE = 1,
    INACTIVE = 2
}

export enum POPULAR {
    ACTIVE = 1,
    INACTIVE = 2
}